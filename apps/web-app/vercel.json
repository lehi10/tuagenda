{
  "buildCommand": "cd ../.. && pnpm install && pnpm db:generate && pnpm --filter db exec prisma migrate deploy && pnpm --filter web-app build",
  "installCommand": "cd ../.. && pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
